apiVersion: v1
kind: Service
metadata:
  name: {{ include "sarvavaidya.fullname" . }}-server
  labels:
    {{- include "sarvavaidya.labels" . | nindent 4 }}
spec:
  type: {{ .Values.server.service.type }}
  ports:
    - port: {{ .Values.server.service.port }}
      targetPort: http
      name: http
  selector:
    app: {{ include "sarvavaidya.fullname" . }}-server
